package application;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.Test;

public class DifficultyValueTest {

    @Test
    public void ethanDifficultyValue() {
        Difficulty easy = Difficulty.EASY;
        Difficulty medium = Difficulty.MEDIUM;
        Difficulty hard = Difficulty.HARD;
        HomeScreen homeScreen = new HomeScreen();
        /*
         * Assert.assertEquals("Health: 200", "Health: " + (50 *
         * homeScreen.setModifier(easy))); Assert.assertEquals("Health: 150", "Health: "
         * + (50 * homeScreen.setModifier(medium))); Assert.assertEquals("Health: 100",
         * "Health: " + (50 * homeScreen.setModifier(hard)));
         * Assert.assertEquals("Health: 0", "Health: " + (50 *
         * homeScreen.setModifier(null))); Assert.assertEquals("Money: 250", "Money: " +
         * (50 * (homeScreen.setModifier(easy) + 1))); Assert.assertEquals("Money: 200",
         * "Money: " + (50 * (homeScreen.setModifier(medium) + 1)));
         * Assert.assertEquals("Money: 150", "Money: " + (50 *
         * (homeScreen.setModifier(hard) + 1))); Assert.assertEquals("Money: 50",
         * "Money: " + (50 * (homeScreen.setModifier(null) + 1)));
         */
    }
}